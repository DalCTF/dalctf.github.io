---
import { Icon } from "astro-icon/components";

interface Props {
  icon: string;
  text: string;
  url: string;
}

const { icon, text, url } = Astro.props;
---

<section>
  <a href={url} target="_blank">
    <div class="left">
      <Icon name={icon} />
      <p class="emphasis">{text}</p>
    </div>
    <div class="right">
      <Icon name="chevron" />
    </div>
  </a>
</section>

<style lang="scss">
  @use "../styles/variables" as *;

  section {
    display: block;
  }

  [data-icon] {
    fill: $foreground-color;
  }

  [data-icon="github"] {
    width: 48px;
    height: 48px;
    padding: 8px;
  }

  [data-icon="discord"] {
    width: 64px;
    height: 64px;
  }

  [data-icon="chevron"] {
    width: 16px;
    height: 16px;
  }

  a {
    background: $background-secondary-color;
    justify-content: space-between;
    border-radius: $radius;
    flex-direction: row;
    align-items: center;
    padding: 16px;
    display: flex;

    color: inherit;
  }

  a:hover .right {
    margin-right: 16px;
  }

  div {
    flex-direction: row;
    align-items: center;
    display: flex;
    gap: 16px;
  }

  .right {
    margin-right: 24px;
    transition: 0.1s;
  }
</style>
